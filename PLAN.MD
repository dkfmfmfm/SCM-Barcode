# BeyondPack SharePoint 상품동기화 개선 계획

> 문서 버전: 1.2  
> 작성일: 2026-08-11  
> 대상 시스템: BEYOND EARTH 품목코드 관리 시스템, Microsoft SharePoint, BeyondPack v1.4  
> 관련 문서: `RESEARCH.MD`  
> 1차 목표: FNSKU 스캔 시 품목코드·SKU·국가명·품목명을 자동 표시하고, 포장정보는 작업자가 직접 입력하는 가벼운 오프라인 우선 포장 프로그램 구축

---

## 문서 지위와 적용 원칙

`PLAN.MD`와 `RESEARCH.MD`는 이 프로그램을 설계·개발·테스트·인수하는 공식 기준 문서다.

- `PLAN.MD`: 1차 개발의 필수 기능, 비기능 요구사항, 테스트, 배포와 인수 기준
- `RESEARCH.MD`: 설계 판단의 근거, 현행 프로그램 분석, 글로벌 사례와 중장기 WMS 확장 기준
- 두 문서가 충돌하면 현재 1차 개발에는 `PLAN.MD`를 우선 적용한다.
- 구현 편의만을 이유로 필수 요구사항을 생략하지 않는다.
- 요구사항이 바뀌면 코드만 수정하지 않고 관련 기준 문서도 같은 PR에서 함께 수정한다.
- PR 설명과 테스트 결과에는 영향을 받은 요구사항 ID를 기록한다.
- 중장기 WMS 기능은 `RESEARCH.MD`를 참고하되, 이번 범위에 임의로 추가하지 않는다.

---

## 1. 결론

1차 개발은 **SharePoint 연계형 상품 마스터 캐시**를 BeyondPack에 추가하는 프로젝트로 정의한다.

- 현재 품목코드 관리 시스템이 상품정보의 원본(Source of Truth)을 유지한다.
- 원본 시스템에서 포장 작업에 필요한 Amazon 상품정보를 SharePoint에 배포한다.
- BeyondPack은 **실행할 때마다 SharePoint의 최신 상품데이터 동기화를 시도**한다.
- 동기화한 데이터는 로컬 SQLite DB에 안전하게 저장한다.
- 작업 중 FNSKU 스캔은 SharePoint가 아니라 로컬 DB를 조회한다.
- 국가명, 품목명, 품목코드, SKU, FNSKU는 자동 표시하고 수정할 수 없게 한다.
- 박스수량, 무게, 가로, 세로, 높이와 박스별 상품수량은 작업자가 직접 입력한다.
- 기존 다품목 합포, 박스 번호, 라벨 출력, Excel 저장·불러오기 기능은 유지한다.

이 방식은 현장 네트워크가 불안정하거나 NAS에 연결되지 않아도 작업을 계속할 수 있으며, 매 스캔마다 SharePoint를 호출하지 않으므로 프로그램의 속도와 단순성을 유지할 수 있다.

---

## 2. 확정된 운영 조건

| 구분 | 확정 내용 | 설계 영향 |
|---|---|---|
| 사업 규모 | 연 매출 약 1,000억 원 | 데이터 오류와 작업중단에 대한 통제가 필요 |
| 상품 특성 | 다품종·소량 포장이 많음 | 검색 속도, 신규 FNSKU 반영, 쉬운 작업화면이 중요 |
| 포장 형태 | 단품 포장과 여러 상품을 한 박스에 넣는 합포가 있음 | 한 박스에 여러 FNSKU와 수량을 기록할 수 있어야 함 |
| 조회 기준 | 같은 FNSKU가 여러 국가에 존재할 수 있음 | 작업자가 국가를 선택하고 `FNSKU|CountryCode` 복합키로 조회 |
| 자동 표시 | 국가명, 품목명, 품목코드, SKU, FNSKU | SharePoint 상품 마스터에서 가져와 읽기 전용 표시 |
| 작업자 입력 | 박스수량, 무게, 가로, 세로, 높이, 박스별 상품수량 | 상품 마스터와 분리해 포장 작업 실적으로 저장 |
| 현장 인프라 | 포장 현장에서 NAS 연결이 어려움 | NAS 실시간 의존 금지, 로컬 캐시 필수 |
| 웹 환경 | NAS를 웹으로 개방하기 어려움 | 사내 NAS 공개 API를 새로 만들지 않음 |
| 연계성 | 품목코드 관리 시스템은 SharePoint와 연결하기 쉬움 | 품목관리 → SharePoint → BeyondPack 단방향 배포 적용 |
| 프로그램 요구 | 빠르고 가벼워야 함 | 실행 중 원격조회 금지, SQLite 인덱스 조회, 기존 UI 유지 |

---

## 3. 프로젝트 목표와 성공 기준

### 3.1 업무 목표

1. Google Sheet 공개 CSV 의존을 제거한다.
2. FNSKU를 스캔하면 정확한 품목코드, SKU, 국가명, 품목명이 자동으로 표시되게 한다.
3. 작업자가 포장정보만 입력하도록 하여 상품정보 오입력을 줄인다.
4. 프로그램 실행 시 최신 상품정보를 자동으로 확인하고 내려받는다.
5. SharePoint 또는 인터넷 장애 시 검증된 기존 상품데이터로 작업할 수 있게 한다.
6. 합포와 기존 라벨·Excel 업무를 중단 없이 유지한다.
7. 향후 WMS를 도입하더라도 상품 조회 모듈을 교체할 수 있는 구조로 만든다.

### 3.2 기술 성공 기준

| 지표 | 목표 |
|---|---:|
| 로컬 FNSKU 조회 p95 | 100ms 이하 |
| FNSKU 정확 일치율 | 100% |
| 중복 FNSKU+국가가 포함된 데이터 배포 | 0건, 전체 업데이트 거부 |
| 작업 국가 미선택 상태의 상품 조회 | 0건, 스캔 전 선택 강제 |
| 필수값이 누락된 활성 상품의 현장 적용 | 0건 |
| 동기화 실패 시 기존 DB 손상·삭제 | 0건 |
| 합포 한 박스 내 다중 FNSKU 기록 | 정상 지원 |
| 기존 라벨 출력·재출력 회귀 오류 | 0건 |
| 기존 프로그램 대비 메모리·실행파일 증가 | 파일럿에서 측정, 원칙적으로 15% 이내 목표 |
| 현장 기본 교육 | 30분 이내에 표준 포장 수행 |

---

## 4. 1차 개발 범위

### 4.1 포함 범위

- SharePoint `Product_Published` List 생성
- 품목코드 관리 시스템에서 SharePoint로 상품정보 단방향 배포
- FNSKU·국가 정규화, 복합키 중복·필수값 검증
- BeyondPack 시작 시 SharePoint 상품데이터 동기화
- 로컬 SQLite 상품 캐시
- 동기화 상태, 상품DB 버전, 최종 성공시간 표시
- 수동 `상품정보 업데이트` 버튼
- FNSKU 조회 결과에 국가명·품목코드·SKU 추가
- 작업 시작 시 국가 선택, 세션 유지, 박스 구성 중 국가 변경 차단
- 자동 표시 필드의 읽기 전용 처리
- 작업자 포장정보 입력 유지
- 합포 작업 유지
- 포장 실적에 조회 당시의 상품정보와 상품DB 버전 저장
- 네트워크·인증·부분 다운로드·잘못된 데이터에 대한 복구 처리
- 기존 Excel 및 라벨 기능 회귀 테스트

### 4.2 제외 범위

- 실시간 재고수량 관리
- 입고예정, PO, ASN, 적치, 로케이션
- 주문 할당, 피킹, 출고확정
- 로트·유통기한·일련번호
- 여러 작업대의 포장 실적 중앙 동시 저장
- SSCC/LPN 기반 물류단위 관리
- SharePoint를 스캔마다 실시간 조회하는 방식
- 포장정보를 SharePoint 상품 마스터에 저장하는 기능
- 품목관리와 SharePoint의 양방향 자유 편집
- 전체 WMS 또는 ERP 교체

위 기능은 `RESEARCH.MD`의 중장기 WMS 로드맵에서 별도로 다룬다. 이번 프로젝트에서는 현장 포장업무를 안정화하는 데 집중한다.

---

## 5. 목표 아키텍처

```mermaid
flowchart LR
    A["품목코드 관리 시스템<br/>상품정보 원본"] -->|승인 상품 단방향 배포| B["SharePoint<br/>Product_Published"]
    B -->|프로그램 실행 시 동기화| C["동기화 모듈<br/>인증·다운로드·검증"]
    C -->|검증 성공 후 원자적 교체| D["로컬 SQLite<br/>상품 캐시"]
    D -->|FNSKU 로컬 조회| E["BeyondPack<br/>포장 작업 화면"]
    E --> F["로컬 포장 실적<br/>Excel·라벨"]
```

### 5.1 책임 분리

| 구성요소 | 책임 | 하지 않는 일 |
|---|---|---|
| 품목코드 관리 시스템 | 품목코드, SKU, FNSKU, 국가, 품목명 생성·수정 | 포장 작업 실적 저장 |
| SharePoint | 승인된 상품정보 공유, 권한, 버전, 변경 이력 | 실시간 스캔 처리, 포장정보 저장 |
| 동기화 모듈 | 최신 데이터 다운로드, 검증, 캐시 교체, 오류 복구 | 상품정보 직접 편집 |
| 로컬 SQLite | 빠른 FNSKU 조회, 오프라인 캐시 | 상품 마스터의 최종 원본 역할 |
| BeyondPack | 포장정보 입력, 합포, 박스관리, 라벨, Excel | 상품정보 직접 수정 |

---

## 6. 상품 데이터 계약

### 6.1 배포 대상

현재 품목관리 프로그램의 행 중 다음 조건을 모두 만족하는 데이터만 SharePoint에 배포한다.

```text
platform = Amazon
active = Y
fnsku가 비어 있지 않음
productCode가 비어 있지 않음
sku가 비어 있지 않음
country 또는 countryName이 비어 있지 않음
품목명이 비어 있지 않음
```

Amazon 이외 플랫폼의 `fnsku` 내부 필드는 실제로 다른 풀필먼트 코드일 수 있으므로, 1차 BeyondPack 데이터에서는 Amazon 행만 사용한다.

### 6.2 SharePoint List

권장 List 이름: `Product_Published`

| 표시명 | 내부명 | 형식 | 필수 | 규칙 |
|---|---|---|:---:|---|
| FNSKU | `FNSKU` | 한 줄 텍스트 | Y | 대문자·공백 제거, 국가별 중복 허용, 인덱스 |
| 품목코드 | `ItemCode` | 한 줄 텍스트 | Y | 원본 `productCode` |
| SKU | `SKU` | 한 줄 텍스트 | Y | 원본 `sku` |
| 국가코드 | `CountryCode` | 한 줄 텍스트 | Y | 원본 `country`, 예: US/JP/DE |
| 조회키 | `LookupKey` | 한 줄 텍스트 | Y | `FNSKU|CountryCode`, 전역 유일, 인덱스 |
| 국가명 | `CountryName` | 한 줄 텍스트 | Y | 원본 `countryName`, 예: 미국/일본/독일 |
| 품목명 | `ProductName` | 한 줄 텍스트 | Y | 기본 `nameKr`, 없으면 `nameEn` |
| 영문 품목명 | `ProductNameEn` | 한 줄 텍스트 | N | 원본 `nameEn` |
| Amazon 계정 | `AmazonAccount` | 한 줄 텍스트 | N | 장애 분석용, 기본 화면에는 숨김 |
| 상태 | `Status` | 선택 | Y | `Published` 또는 `Inactive` |
| 원본 수정시각 | `SourceModifiedAt` | 날짜/시간 | Y | 품목관리 시스템 기준 |
| 데이터 버전 | `DataVersion` | 한 줄 텍스트 | Y | 배포 단위 버전 |
| 스키마 버전 | `SchemaVersion` | 숫자 | Y | 복합키 도입 버전 `2` |

### 6.3 필드 소유권

| 필드 | 수정 원본 | SharePoint 직접 수정 |
|---|---|---|
| FNSKU | 품목코드 관리 시스템 | 금지 |
| 품목코드 | 품목코드 관리 시스템 | 금지 |
| SKU | 품목코드 관리 시스템 | 금지 |
| 국가코드·국가명 | 품목코드 관리 시스템 | 금지 |
| 품목명 | 품목코드 관리 시스템 | 금지 |
| 승인·게시 상태 | SharePoint 또는 지정 승인 절차 | 허용 |
| 변경 의견·관련 문서 | SharePoint | 허용 |
| 포장정보 | BeyondPack 작업 실적 | SharePoint 상품 List에 저장하지 않음 |

동일한 핵심 상품 필드를 두 시스템에서 모두 수정할 수 있게 만들지 않는다.

---

## 7. FNSKU 조회 규칙

### 7.1 정규화

스캔값과 마스터값 모두 아래 순서로 정규화한다.

```text
1. 앞뒤 공백 제거
2. 탭·개행 제거
3. 영문 대문자로 변환
4. 빈 문자열 차단
```

예:

```text
입력: " x003abc123\r\n"
조회: "X003ABC123"
```

### 7.2 일치 정책

- FNSKU는 부분검색이 아닌 정확 일치만 허용한다.
- 작업자는 스캔 전에 국가를 선택하며 선택값은 현재 작업 세션 동안 유지한다.
- 동일 FNSKU가 다른 국가에 존재하는 것은 정상 데이터로 허용한다.
- 동일 `FNSKU|CountryCode`가 두 행 이상이면 최신 행을 임의 선택하지 않고 데이터 전체 업데이트를 실패 처리한다.
- 박스에 구성품이 추가된 뒤에는 확정 또는 초기화 전까지 국가를 변경할 수 없다.
- `Inactive` 상품은 조회 결과를 표시하지 않고 사용중지 메시지를 보여준다.
- 조회되지 않으면 임의 상품 선택이나 수동 품목명 입력을 허용하지 않는다.
- 미등록 FNSKU 화면에는 `다시 스캔`, `상품정보 업데이트`, `관리자에게 문의`만 제공한다.

### 7.3 자동 표시 결과

```text
국가명     미국
품목명     DONGWON YANGBAN SEAWEED 12 PACK
품목코드   A000000120
SKU        US-AMZ-SEAWEED-12P
FNSKU      X003ABC123
```

국가명, 품목명, 품목코드, SKU, FNSKU는 읽기 전용이다.

---

## 8. 포장 작업 데이터 모델

### 8.1 상품 마스터와 포장정보 분리

| 자동 조회 상품정보 | 작업자 입력 포장정보 |
|---|---|
| 국가명 | 박스수량 |
| 품목명 | 무게 |
| 품목코드 | 가로 |
| SKU | 세로 |
| FNSKU | 높이 |
| 상품DB 버전 | 박스당 각 상품수량 |

무게와 치수의 단위는 화면과 저장데이터에서 고정한다.

- 무게: `kg`
- 가로·세로·높이: `cm`
- 박스수량·상품수량: 양의 정수

### 8.2 합포 구조

한 박스 또는 동일 구성의 박스 그룹에 여러 FNSKU를 추가할 수 있어야 한다.

```text
박스 그룹
├─ 박스수량: 10 BOX
├─ 무게: 8.4 kg / BOX
├─ 규격: 42 × 31 × 24 cm
└─ 구성품
   ├─ X003ABC123 / A000000120 / SKU-A / 2 EA
   ├─ X004DEF456 / A000000230 / SKU-B / 1 EA
   └─ X005GHI789 / A000000340 / SKU-C / 3 EA
```

각 FNSKU를 추가할 때 해당 상품의 국가명·품목명·품목코드·SKU를 상품DB에서 조회하고, 작업 실적에는 조회 결과의 스냅샷을 함께 저장한다. 이후 품목명이 수정되어도 과거 포장 당시 정보가 바뀌지 않아야 한다.

### 8.3 최소 저장 필드

#### PackagingJob

```text
job_id
created_at
operator_id 또는 operator_name
product_db_version
app_version
status
```

#### BoxGroup

```text
box_group_id
job_id
box_start_no
box_count
weight_kg
length_cm
width_cm
height_cm
created_at
```

#### BoxItem

```text
box_group_id
fnsku
item_code
sku
country_code
country_name
product_name
qty_per_box
source_modified_at
```

1차 버전에서는 기존 Excel 저장 형식을 유지하되, 새 필드를 손실 없이 포함해야 한다.

---

## 9. 프로그램 시작 동기화 사양

### 9.1 기본 동작

BeyondPack은 실행할 때마다 SharePoint 동기화를 시도한다.

```text
프로그램 실행
  → 로컬 DB 존재 여부 확인
  → SharePoint 인증
  → Published 상품 전체 또는 변경분 다운로드
  → 임시 DB 작성
  → 검증
  → 검증 성공 시 로컬 DB 원자적 교체
  → 포장 화면 진입
```

1차 MVP는 구현과 검증이 단순한 **Published 상품 전체 동기화**로 시작한다. 운영 데이터량과 작업대 수가 증가하면 `Modified` 또는 Microsoft Graph delta를 이용한 변경분 동기화로 전환한다.

### 9.2 로컬 캐시 파일

권장 파일:

```text
data/
├─ products.db
├─ products.previous.db
└─ sync-status.json
```

- 제품 조회에는 Python 표준 `sqlite3`를 사용한다.
- FNSKU 정규화 컬럼에는 일반 인덱스, `FNSKU|CountryCode` LookupKey에는 `UNIQUE INDEX`를 설정한다.
- 새 DB는 `products.new.db`로 만든 뒤 검증 완료 시 교체한다.
- 교체 전 기존 DB를 `products.previous.db`로 한 세대 보관한다.
- 다운로드 중 프로그램이 종료되어도 기존 `products.db`는 손상되지 않아야 한다.

### 9.3 동기화 검증

아래 중 하나라도 실패하면 새 데이터를 적용하지 않는다.

- SharePoint 응답 또는 인증 실패
- 페이지 일부만 다운로드됨
- 스키마 버전 불일치
- 동일 FNSKU+국가 복합키 중복
- FNSKU, 품목코드, SKU, 국가, 품목명 필수값 누락
- 레코드 수가 0
- JSON/응답 파싱 실패
- SQLite 트랜잭션 실패
- 이전 버전 대비 비정상적으로 큰 데이터 감소

마지막 항목의 초기 경고 기준은 `20% 이상 감소`로 제안하며, 실제 상품 등록·비활성화 패턴을 본 후 조정한다.

### 9.4 동기화 상태

| 상태 | 화면 | 작업 허용 |
|---|---|:---:|
| `CURRENT` | 초록: 최신 상품정보 | 허용 |
| `CACHED` | 주황: 이전 상품정보 사용 중 | 조건부 허용 |
| `SYNCING` | 파랑: 상품정보 업데이트 중 | 로컬 데이터가 있으면 허용 가능 |
| `ERROR` | 빨강: 업데이트 실패 | 캐시 정책에 따라 결정 |
| `NO_DATA` | 빨강: 사용할 상품정보 없음 | 차단 |

### 9.5 캐시 유효기간 제안

| 마지막 성공 동기화 | 기본 정책 |
|---|---|
| 24시간 이내 | 경고 후 기존 데이터 사용 허용 |
| 24~72시간 | 관리자 확인 후 사용 허용 |
| 72시간 초과 | 기본 차단, 책임자 승인 필요 |
| 최초 실행·캐시 없음 | 작업 차단 |

시간 기준은 파일럿에서 신규 상품 등록 빈도와 긴급 출고 절차를 확인한 뒤 확정한다.

### 9.6 수동 동기화

메인 화면에 `상품정보 업데이트` 버튼을 제공한다.

- 신규 FNSKU가 조회되지 않을 때 즉시 실행할 수 있어야 한다.
- 중복 클릭은 하나의 동기화 작업으로 합친다.
- 완료 결과, 적용 버전, 상품 건수, 오류를 사용자에게 보여준다.
- 동기화가 실패해도 현재 작성 중인 박스 정보는 사라지지 않는다.

---

## 10. SharePoint 인증과 권한

### 10.1 금지사항

- Microsoft 계정 비밀번호를 소스코드·설정파일·EXE에 포함하지 않는다.
- Client Secret을 데스크톱 EXE에 하드코딩하지 않는다.
- 공개 CSV 또는 익명 링크를 사용하지 않는다.
- 포장 프로그램에 SharePoint 쓰기·삭제 권한을 부여하지 않는다.
- 전체 SharePoint 사이트에 불필요한 광역 권한을 부여하지 않는다.

### 10.2 인증 방식 결정

| 현장 조건 | 권장 방식 |
|---|---|
| 작업자별 Microsoft 365 계정 사용 가능 | MSAL 사용자 로그인 및 Windows 토큰 캐시 |
| 공용 포장 PC이며 작업자 계정 사용 곤란 | Entra ID App-Only + 인증서, 지정 사이트/List 읽기 전용 |

공용 PC용 인증서를 사용한다면 개인키를 Windows 인증서 저장소에 비내보내기 형태로 설치하고, 앱 권한을 `Product_Published`가 있는 지정 사이트 또는 List에만 제한한다.

### 10.3 최소 권한

- BeyondPack: `Product_Published` 읽기 전용
- 품목관리 연동: `Product_Published` 추가·수정 권한
- 승인자: 게시상태 승인 권한
- 작업자: SharePoint 직접 편집 권한 없음
- 관리자: 앱 등록, 인증서 교체, 감사로그 확인

---

## 11. UI 변경 계획

### 11.1 상품 조회 영역

기존 상품정보 영역에 아래 필드를 배치한다.

```text
[상품DB: 최신 | 2026.08.11.04 | 18,452개]

FNSKU      [X003ABC123                ]
국가명     [미국                       ] 읽기 전용
품목명     [DONGWON YANGBAN SEAWEED   ] 읽기 전용
품목코드   [A000000120                 ] 읽기 전용
SKU        [US-AMZ-SEAWEED-12P        ] 읽기 전용
```

표시 우선순위는 `품목명 → 품목코드 → SKU → FNSKU → 국가명`으로 한다. 실제 배치는 현재 화면 크기와 작업자 시선 흐름을 기준으로 파일럿에서 조정한다.

### 11.2 포장정보 영역

```text
박스수량     [    ] BOX
무게         [    ] kg / BOX
가로         [    ] cm
세로         [    ] cm
높이         [    ] cm
상품수량     [    ] EA / BOX
```

- 필수 숫자 입력은 빈 값, 0 이하, 문자, 비정상 소수점을 차단한다.
- 소수점 허용 범위는 무게와 치수에만 적용한다.
- Enter 중심으로 다음 입력칸으로 이동하게 한다.
- 스캐너 입력 후 마우스 조작 없이 포장정보를 입력할 수 있어야 한다.

### 11.3 오류 메시지

| 상황 | 메시지 |
|---|---|
| FNSKU 미등록 | `등록되지 않은 FNSKU입니다. 상품정보를 업데이트하거나 관리자에게 문의하세요.` |
| 사용중지 | `사용중지된 상품입니다. 포장할 수 없습니다.` |
| 동기화 실패 | `최신 상품정보를 가져오지 못했습니다. 기존 버전을 사용합니다.` |
| 국가 미선택 | `먼저 작업 국가를 선택하세요.` |
| 선택 국가에 상품 없음 | `선택한 국가에는 등록되지 않은 FNSKU입니다. 등록 국가를 확인하세요.` |
| 중복 복합키 | `상품 마스터에 동일 FNSKU+국가가 2건 이상 있습니다. 새 데이터 적용을 중단했습니다.` |
| 필수값 누락 | `FNSKU에 연결된 품목코드/SKU/국가명/품목명이 누락되었습니다.` |

---

## 12. 프로그램 경량화 원칙

1. 스캔마다 SharePoint API를 호출하지 않는다.
2. 상품 조회를 위해 Excel 전체 또는 pandas DataFrame을 반복 검색하지 않는다.
3. 상품 캐시는 SQLite로 관리하고 FNSKU 인덱스를 사용한다.
4. 네트워크 동기화는 별도 작업으로 실행해 UI가 멈추지 않게 한다.
5. SharePoint에서 필요한 컬럼만 내려받는다.
6. 이미지와 문서를 상품 캐시에 포함하지 않는다.
7. 원격 오류 재시도 횟수와 시간을 제한한다.
8. 동기화 로그를 무제한 누적하지 않고 보관기간을 설정한다.
9. 기존 PySide6 화면과 프린터 기능을 최대한 유지해 재개발 범위를 줄인다.
10. SharePoint 연동부를 `ProductSource` 인터페이스로 분리해 향후 WMS API로 교체할 수 있게 한다.

권장 모듈 경계:

```text
ProductSource
├─ SharePointProductSource
└─ FutureWmsProductSource

ProductSyncService
ProductCacheRepository
FnskuLookupService
PackagingService
PrintService
```

---

## 13. 오류와 복구 정책

| 장애 | 처리 |
|---|---|
| 인터넷 연결 없음 | 기존 로컬 DB 사용, 상태 경고 |
| Microsoft 로그인 만료 | 재로그인 안내, 기존 DB 유지 |
| SharePoint 권한 없음 | 작업자 메시지와 관리자용 상세 오류 분리 |
| API 429/503 | `Retry-After` 준수, 제한된 횟수로 재시도 |
| 다운로드 중 종료 | 임시파일 폐기, 기존 DB 유지 |
| 동일 FNSKU+국가 중복 | 새 데이터 전체 적용 중단 |
| 필수값 누락 | 해당 데이터 배포 실패 리포트 생성, 새 DB 미적용 |
| 로컬 DB 손상 | 이전 DB 자동 복구 시도 |
| 신규 FNSKU 미조회 | 수동 업데이트 제공, 임의 입력 차단 |
| 프린터 오류 | 기존 재출력 흐름 유지, 상품 동기화와 분리 |

오류로그에는 계정 토큰, 인증서, 개인정보, SharePoint 응답 전체를 남기지 않는다.

---

## 14. 단계별 구현 로드맵

예상 기간은 Microsoft 365 관리자 협조와 BeyondPack 원본 소스 접근이 가능하다는 전제의 4~6주다.

### Phase 0 — 요구사항·기준선 확정 (2~3일)

- [ ] 실제 활성 Amazon FNSKU 건수 확인
- [ ] 포장 PC 수, Windows 버전, 인터넷 접속 가능 여부 확인
- [ ] 작업자별 M365 계정 사용 가능 여부 확인
- [ ] 현행 BeyondPack 시작시간, 메모리, FNSKU 조회시간 측정
- [ ] 현행 Excel 출력 컬럼과 라벨 샘플 확보
- [ ] 박스당 상품수량 입력 방식과 합포 작업 절차 현장 확인
- [ ] 캐시 유효기간과 관리자 예외권한 확정
- [ ] SharePoint 사이트·List 소유부서 지정

**완료 기준:** 데이터 소유자, 인증 방식, 필드 정의, 운영 예외정책 승인.

### Phase 1 — SharePoint 상품 배포 기반 (3~5일)

- [ ] `Product_Published` List 생성
- [ ] FNSKU 일반 인덱스와 LookupKey 고유값 설정
- [ ] 필수 컬럼·상태·스키마 버전 설정
- [ ] 읽기·쓰기·승인 권한 분리
- [ ] Entra ID 앱 등록 및 최소 권한 승인
- [ ] 테스트 상품 50~100건 게시
- [ ] 변경·승인·비활성화 이력 확인

**완료 기준:** 테스트 FNSKU의 품목코드·SKU·국가명·품목명이 API로 정확히 조회됨.

### Phase 2 — 품목관리 시스템 연계 (3~7일)

- [ ] 기존 필드와 SharePoint 컬럼 매핑
- [ ] Amazon·활성·FNSKU 보유 행만 추출
- [ ] FNSKU 대문자·공백 정규화
- [ ] 중복·필수값 사전검증
- [ ] 신규·수정·비활성 데이터 단방향 반영
- [ ] 배포 버전과 원본 수정시각 기록
- [ ] 배포 실패 리포트와 재실행 기능
- [ ] 핵심 상품정보의 SharePoint 직접편집 방지

**완료 기준:** 품목관리 시스템의 승인 데이터와 SharePoint 게시 데이터가 100% 대조 일치.

### Phase 3 — BeyondPack 동기화·조회 개선 (7~10일)

- [ ] SharePoint 인증 모듈
- [ ] 프로그램 실행 시 자동 동기화
- [ ] API 페이지네이션과 제한 재시도
- [ ] 임시 DB 생성·검증·원자적 교체
- [ ] 로컬 SQLite FNSKU 인덱스
- [ ] 상품DB 상태·버전·최종 동기화시간 표시
- [ ] `상품정보 업데이트` 버튼
- [ ] 국가명·품목코드·SKU 표시 추가
- [ ] 자동 필드 읽기 전용 처리
- [ ] 합포의 각 FNSKU 상품정보 스냅샷 저장
- [ ] Excel 출력에 국가명·품목코드·SKU·DB 버전 추가
- [ ] 기존 프린터·라벨·박스 수정·재출력 기능 회귀 확인

**완료 기준:** 인터넷 연결과 무관하게 캐시된 상품의 스캔 조회가 가능하고, 신규 필드가 화면·Excel에 정확히 표시됨.

### Phase 4 — QA·현장 파일럿 (5~7일)

- [ ] 개발 PC 자동 테스트
- [ ] 포장 PC 1대에서 파일럿
- [ ] 실제 스캐너·프린터 연동
- [ ] 단품·합포·동일박스 복수수량 테스트
- [ ] 네트워크 차단·복구 테스트
- [ ] SharePoint 권한 만료 테스트
- [ ] 중복·누락·부분 다운로드 테스트
- [ ] 작업자 2명 이상 UAT
- [ ] 기존 버전 대비 처리시간과 오류율 비교
- [ ] 수정사항 반영 후 운영 승인

**완료 기준:** 치명적 오류 0건, P1 오류 0건, 작업자 승인, 기존 작업속도 이상.

### Phase 5 — 단계 배포·안정화 (1주)

- [ ] 1대 → 3~5대 → 전체 포장 PC 순차 배포
- [ ] v1.4 및 기존 상품DB 롤백 패키지 보관
- [ ] 앱 버전과 상품DB 버전 수집
- [ ] 첫 1주 일일 오류·미등록 FNSKU 검토
- [ ] 첫 1개월 주간 지표 검토
- [ ] 운영 매뉴얼과 관리자 장애대응 문서 배포

**완료 기준:** 전체 작업대 정상 사용, Google Sheet 의존 제거, 긴급 롤백 가능.

---

## 15. 테스트 계획

### 15.1 상품 데이터 테스트

- [ ] 정상 FNSKU 조회
- [ ] 소문자·앞뒤 공백 포함 스캔
- [ ] 동일 FNSKU·다른 국가 2건 정상 조회
- [ ] 동일 FNSKU+국가 중복 2건 차단
- [ ] 품목코드 누락
- [ ] SKU 누락
- [ ] 국가코드 또는 국가명 누락
- [ ] 품목명 누락
- [ ] 비활성 상품
- [ ] Amazon 이외 플랫폼 행 제외
- [ ] 한 품목코드에 여러 Amazon SKU/FNSKU가 있는 경우
- [ ] 한글·영문·특수문자 품목명

### 15.2 동기화 테스트

- [ ] 최초 실행 정상 다운로드
- [ ] 두 번째 실행 동일 데이터 다운로드
- [ ] 신규 상품 추가 후 실행
- [ ] SKU·국가명 수정 후 실행
- [ ] 상품 비활성화 후 실행
- [ ] 인터넷 없는 상태로 실행
- [ ] 인증 만료 상태로 실행
- [ ] 다운로드 중 강제 종료
- [ ] API 429·503 응답
- [ ] 페이지 중 일부 실패
- [ ] 잘못된 스키마 버전
- [ ] 이전 DB 자동 복구

### 15.3 포장 업무 테스트

- [ ] 단일 FNSKU 단일박스
- [ ] 단일 FNSKU 복수박스
- [ ] 한 박스에 여러 FNSKU 합포
- [ ] 합포 구성품별 수량 입력
- [ ] 포장정보 수정·삭제
- [ ] 박스 번호 증가·감소
- [ ] 라벨 최초출력·재출력
- [ ] Excel 저장·불러오기
- [ ] 작업 중 상품정보 수동 업데이트
- [ ] 미등록 FNSKU 임의 작업 차단

### 15.4 성능 테스트

- [ ] 실제 운영 데이터 전체 동기화 시간
- [ ] 로컬 1만·5만·10만 FNSKU 조회시간
- [ ] 연속 100회 스캔 중 UI 멈춤 여부
- [ ] 기존 대비 실행파일 크기와 메모리
- [ ] 포장 PC 저사양 환경 실행

---

## 16. 사용자 인수 기준

아래를 모두 만족해야 운영 배포한다.

1. FNSKU 스캔 시 정확한 국가명, 품목명, 품목코드, SKU, FNSKU가 표시된다.
2. 자동 표시 상품정보는 작업자가 수정할 수 없다.
3. 박스수량, 무게, 가로, 세로, 높이, 상품수량은 작업자가 입력할 수 있다.
4. 한 박스에 여러 FNSKU를 추가할 수 있다.
5. 포장 실적에 각 FNSKU의 품목코드, SKU, 국가명이 저장된다.
6. 프로그램을 다시 실행할 때 SharePoint 최신 데이터 동기화를 시도한다.
7. 동기화 실패 시 기존 상품DB가 삭제되거나 손상되지 않는다.
8. 캐시 사용 여부와 마지막 동기화시간이 화면에서 확인된다.
9. 신규 FNSKU는 수동 업데이트 후 프로그램 재실행 없이 조회할 수 있다.
10. 미등록·중복·비활성 FNSKU는 작업을 차단한다.
11. 기존 박스 라벨 출력과 재출력이 정상 동작한다.
12. 기존 Excel 파일의 주요 업무 호환성을 유지한다.

---

## 17. 배포와 롤백

### 17.1 배포 패키지

```text
BeyondPack-vNext/
├─ BeyondPack.exe
├─ config.json
├─ data/products.db
├─ docs/사용자매뉴얼.pdf
└─ version.json
```

인증서가 필요한 경우 EXE 폴더에 개인키 파일을 두지 않고 Windows 인증서 저장소와 배포 절차를 사용한다.

### 17.2 롤백

- 기존 BeyondPack v1.4 실행파일과 설정 백업
- 새 버전 배포 전 현재 작업파일 백업
- `products.previous.db` 한 세대 유지
- 새 버전 장애 시 v1.4 또는 직전 안정 버전으로 복귀
- 롤백 후에도 작업 중 생성한 Excel과 라벨 이력을 보존
- 상품DB 버전과 앱 버전을 별도로 관리

---

## 18. 운영 지표

### 상품 데이터

- SharePoint 마지막 게시시간
- BeyondPack 마지막 동기화 성공시간
- 상품DB 버전
- 게시 상품 수
- 미등록 FNSKU 발생 건수
- 중복·필수값 누락으로 거부된 건수
- 캐시 데이터로 작업한 횟수

### 포장 작업

- 시간당 처리 박스 수
- 합포 박스 비율
- FNSKU 재스캔율
- 상품정보 오류 문의 건수
- 포장정보 수정률
- 라벨 재출력률과 사유
- 프로그램 시작 및 동기화 평균시간

파일럿 전후 최소 2주씩 같은 지표를 비교해 개선효과를 판단한다.

---

## 19. 역할과 책임

| 역할 | 책임 |
|---|---|
| 품목정보 담당자 | FNSKU, 품목코드, SKU, 국가명, 품목명 정확성 |
| 승인자 | SharePoint 게시 승인, 대량 비활성화 확인 |
| Microsoft 365 관리자 | Entra 앱, SharePoint 권한, 인증서 관리 |
| 개발자 | 동기화, 캐시, UI, 오류복구, 배포 패키지 |
| 포장현장 책임자 | 입력 규칙, 캐시 예외 승인, 파일럿·교육 |
| 작업자 | 포장정보 입력, 미등록 상품 관리자 보고 |
| 운영관리자 | 버전, 로그, 장애, 롤백, 지표 모니터링 |

---

## 20. 남은 의사결정

| 항목 | 권장 기본안 | 확정 주체 |
|---|---|---|
| SharePoint 사이트·List 위치 | 전용 사이트의 `Product_Published` | M365 관리자·품목팀 |
| 인증 방식 | 공용 PC이면 제한된 App-Only 인증서 | M365 관리자·보안 |
| 프로그램 시작 정책 | 자동 동기화 시도 + 캐시 fallback | 현장 책임자 |
| 캐시 유효기간 | 24시간/72시간 단계 경고 | 현장 책임자 |
| 표시 품목명 | 한글명 우선, 없으면 영문명 | 품목팀 |
| 치수·무게 단위 | cm, kg 고정 | 물류팀 |
| 박스당 상품수량 | FNSKU별 작업자 입력 | 물류팀 |
| 포장 실적 중앙수집 | 1차 제외, 로컬 Excel 유지 | 경영·물류·개발 |
| 상품 전체/변경분 동기화 | 1차 전체, 성장 시 delta | 개발·운영 |

---

## 21. 중장기 확장 경계

이번 프로젝트는 포장 현장의 상품조회 안정화 단계다. 다음 요건이 발생하면 BeyondPack에 계속 기능을 누적하지 않고 별도 WMS 단계로 전환한다.

- 여러 작업대의 실적을 실시간 통합해야 할 때
- 주문과 박스 내용물을 자동 대조해야 할 때
- 재고수량과 입출고를 중앙에서 관리해야 할 때
- 창고·로케이션·로트·유통기한 추적이 필요할 때
- 박스·팔레트 LPN/SSCC가 필요할 때
- 피킹·패킹 작업지시와 작업자 생산성을 관리해야 할 때
- 인터넷 단절 중 발생한 거래를 중앙 원장에 안전하게 재전송해야 할 때

향후에는 아래처럼 상품소스만 교체할 수 있어야 한다.

```text
현재: SharePointProductSource → 로컬 캐시 → BeyondPack
향후: WmsProductSource       → 로컬 캐시 → Scan Workbench
```

---

## 22. 최종 실행 순서

1. SharePoint 필드와 인증 방식을 확정한다.
2. 실제 운영 데이터를 추출해 FNSKU+국가 복합키 중복·누락을 먼저 정리한다.
3. 품목관리 시스템에서 `Product_Published`로 단방향 배포한다.
4. BeyondPack에 안전한 시작 동기화와 로컬 SQLite 캐시를 추가한다.
5. 국가명·품목코드·SKU 자동 표시와 읽기 전용 처리를 적용한다.
6. 작업자 포장정보 입력과 합포 기능을 유지한다.
7. Excel·라벨·프린터 회귀 테스트를 완료한다.
8. 포장 PC 1대에서 실제 작업 파일럿을 진행한다.
9. 단계적으로 전체 작업대에 배포한다.
10. 운영지표를 수집해 다음 WMS 투자 시점을 판단한다.

---

## 23. 현장 편의성·제품 완성도·상업성 기준

### 23.1 현장 UX 목표

현장 작업자는 프로그램의 구조를 이해할 필요가 없어야 한다. 화면은 항상 **지금 해야 할 한 가지 행동**, **정상 처리 여부**, **문제가 생겼을 때 다음 행동**을 명확하게 보여줘야 한다.

표준 작업 흐름:

```text
FNSKU 스캔
  → 국가명·품목명·품목코드·SKU 자동 표시
  → 합포 구성품과 수량 확인
  → 박스수량·무게·치수 입력
  → 검증
  → 저장·라벨 출력
  → 다음 작업 입력칸으로 자동 이동
```

#### 필수 UX 원칙

| 요구사항 ID | 우선순위 | 기준 | 인수 방법 |
|---|:---:|---|---|
| `UX-001` | P0 | 정상 작업은 마우스 없이 스캐너와 키보드만으로 완료 | 실제 작업 20건 연속 수행 |
| `UX-002` | P0 | 현재 입력해야 하는 칸에 항상 포커스가 위치 | 화면 전환·오류·저장 후 포커스 확인 |
| `UX-003` | P0 | 성공은 초록·짧은 고음, 실패는 빨강·저음으로 즉시 구분 | 시각·소리 각각 끈 상태도 시험 |
| `UX-004` | P0 | 품목명·품목코드·SKU·FNSKU·국가명이 큰 글씨로 구분 | 1m 거리와 일반 작업 조명에서 확인 |
| `UX-005` | P0 | 잘못된 값은 저장 버튼을 누른 뒤가 아니라 입력 즉시 표시 | 0·음수·문자·누락값 시험 |
| `UX-006` | P0 | 미등록·중복·비활성 FNSKU의 임의 진행을 차단 | 예외 데이터 UAT |
| `UX-007` | P0 | 합포 구성품은 품목별 카드 또는 행으로 명확히 분리 | 2·5·10개 FNSKU 합포 시험 |
| `UX-008` | P1 | 저장 직전 박스수량·치수·중량·구성품을 한 화면에서 최종 확인 | 오입력 시나리오 시험 |
| `UX-009` | P1 | 작업 취소·수정 후 이전 단계로 안전하게 복귀 | 작업 상태 손실 여부 확인 |
| `UX-010` | P1 | 프린터 오류 후 포장내용 재입력 없이 재출력 | 용지없음·오프라인 시험 |
| `UX-011` | P1 | 신규 작업자용 단계별 도움말과 연습모드 제공 | 30분 교육 후 독립작업 평가 |
| `UX-012` | P2 | 현장별 단축키·글자크기·알림음을 설정 가능 | 설정 저장·복원 시험 |

Microsoft Dynamics 365의 창고 모바일 앱도 각 단계에 짧은 제목, 아이콘, 구체적인 지시를 제공하고, 예외 작업 후 원래 작업으로 돌아오는 흐름을 지원한다. BeyondPack에도 이 원칙을 적용한다.

### 23.2 작업 속도를 높이는 구체 기능

#### P0 — 첫 배포에 반드시 포함

1. **자동 포커스**
   - 실행 직후 국가 미선택이면 국가 선택칸, 선택 완료 후 FNSKU 입력칸 활성화
   - 스캔 성공 후 상품수량 또는 첫 포장정보 입력칸으로 이동
   - 저장 후 다음 FNSKU 입력칸으로 복귀

2. **스캔 버퍼 처리**
   - 스캐너가 매우 빠르게 문자를 입력해도 누락되지 않게 한다.
   - Enter, Tab, CR/LF 등 스캐너 종료문자를 설정할 수 있게 한다.
   - 같은 FNSKU가 짧은 시간에 중복 스캔되면 중복 여부를 확인한다.

3. **입력값 기본 검증**
   - 박스수량과 상품수량: 1 이상의 정수
   - 무게와 치수: 0보다 큰 숫자
   - 단위: kg, cm로 고정
   - 소수점 자리수와 허용 최대값을 설정에서 관리

4. **읽기 전용 상품정보**
   - 품목명, 품목코드, SKU, 국가명은 작업자가 수정하지 못한다.
   - 상품정보가 틀리면 현장에서 임시 수정하지 않고 오류신고를 남긴다.

5. **명확한 다음 행동**
   - `조회 실패`가 아니라 `상품정보 업데이트 후 다시 스캔하세요`처럼 해결방법을 표시한다.
   - 오류 메시지마다 관리자용 오류코드를 함께 제공한다.

6. **작업 중 자동 임시저장**
   - FNSKU 추가, 포장정보 변경, 박스 확정 시점마다 임시저장한다.
   - 비정상 종료 후 마지막 미완료 작업을 복구할 수 있어야 한다.

#### P1 — 파일럿 직후 개선

1. **최근 포장값 제안**
   - 같은 FNSKU의 최근 무게·치수를 참고값으로 보여준다.
   - 자동 확정하지 않고 작업자가 실제 박스를 측정해 확인하게 한다.
   - 과거값과 현재 입력값 차이가 설정범위를 넘으면 경고한다.

2. **합포 템플릿**
   - 반복되는 합포 구성을 즐겨찾기로 저장한다.
   - 템플릿 적용 후 실제 FNSKU와 수량을 다시 확인한다.

3. **박스 복제**
   - 동일 구성의 박스를 여러 개 만드는 경우 구성품과 치수를 복제한다.
   - 박스번호는 중복되지 않게 새로 생성한다.

4. **실수 복구**
   - 마지막 작업 1회 취소와 복원 기능을 제공한다.
   - 삭제·수정·재출력에는 작업자, 시간, 사유를 기록한다.

5. **단축키 기본안**

   | 단축키 | 동작 |
   |---|---|
   | `F2` | 상품정보 업데이트 |
   | `F4` | 현재 입력 초기화 |
   | `Ctrl+Enter` | 박스 확정 |
   | `F8` | 마지막 라벨 재출력 |
   | `Esc` | 현재 팝업 닫기 또는 안전한 이전 단계 |

단축키는 Windows·스캐너·프린터 드라이버와 충돌 여부를 확인한 후 확정한다.

### 23.3 프로그램 완성도를 높이는 필수 요소

기능이 동작하는 것만으로 완성된 제품이 되지 않는다. 설치, 업데이트, 장애복구, 진단, 보안과 지원까지 포함해야 한다.

| 요구사항 ID | 영역 | 필수 기준 |
|---|---|---|
| `QLT-001` | 자동저장 | 비정상 종료 후 미완료 작업 복구 |
| `QLT-002` | 업데이트 | 앱 버전과 상품DB 버전을 별도로 관리 |
| `QLT-003` | 롤백 | 직전 안정 앱과 상품DB로 복귀 가능 |
| `QLT-004` | 진단 | 관리자용 진단파일을 한 번에 생성하되 인증정보 제외 |
| `QLT-005` | 로깅 | 동기화·조회실패·수정·삭제·재출력 기록 |
| `QLT-006` | 설정 | 프린터·라벨·단위·알림 설정 백업·복원 |
| `QLT-007` | 프린터 | 테스트 출력, 상태확인, 실패 후 안전한 재출력 |
| `QLT-008` | 설치 | 설치·업데이트·제거 후 사용자 데이터 보존 |
| `QLT-009` | 보안 | 비밀번호·Client Secret 하드코딩 금지, 최소권한 적용 |
| `QLT-010` | 호환성 | 지원 Windows·스캐너·프린터 목록과 검증 버전 관리 |
| `QLT-011` | 성능 | 연속 스캔 중 UI 정지 1초 이상 0건 |
| `QLT-012` | 데이터 | Excel·라벨·로컬 DB의 품목코드/SKU/FNSKU 일치 |
| `QLT-013` | 테스트 | 기능별 자동테스트와 실제 장비 회귀테스트 통과 |
| `QLT-014` | 문서 | 작업자 1페이지 가이드와 관리자 장애대응서 제공 |
| `QLT-015` | 접근성 | 색상 외 소리·문자·아이콘으로 상태를 중복 전달 |

#### 시작 전 자체 점검

프로그램은 작업 화면을 열기 전에 다음 항목을 자동 확인한다.

```text
상품DB 존재·정상 여부
마지막 동기화 시간
프린터 설치·온라인 여부
라벨 설정 유효성
저장폴더 쓰기 가능 여부
필수 설정 누락 여부
프로그램 버전 지원 여부
```

점검 실패가 포장을 막아야 하는 항목과 경고만 할 항목을 구분한다. 예를 들어 상품DB 없음은 차단하지만 프린터 오프라인은 라벨 출력만 차단하고 작업 임시저장은 허용할 수 있다.

#### 배포 신뢰성

- 사내 배포라도 설치파일과 EXE에 회사 코드서명을 적용한다.
- 설치 패키지는 버전, 배포일, 변경내용, 해시를 제공한다.
- 업데이트 중 전원이 꺼져도 기존 안정 버전이 실행돼야 한다.
- 자동 업데이트는 작업 중 실행하지 않고 프로그램 시작 전 또는 종료 후 적용한다.
- 상업 배포를 고려하면 MSIX 또는 서명된 MSI/EXE와 안전한 자동 업데이트 체계를 검토한다.

### 23.4 상업적 가치 — 내부 운영 관점

내부 사용의 사업가치는 단순히 “조회 필드가 늘었다”가 아니라 작업시간, 오류비용, 교육비와 중단시간 감소로 측정한다.

#### 핵심 ROI 지표

```text
연간 작업시간 절감액
= 작업 1건당 절감초 × 연간 작업건수 × 시간당 인건비 ÷ 3,600

연간 오류비용 절감액
= 개선 전 오포장건수 × 건당 평균 손실
 - 개선 후 오포장건수 × 건당 평균 손실

총 운영효과
= 작업시간 절감 + 오류비용 절감 + 교육시간 절감 + 작업중단 감소
```

파일럿 전후로 다음 수치를 반드시 측정한다.

- 박스 1개 처리시간
- 합포 1건 처리시간
- FNSKU 조회 실패율
- SKU·품목코드 확인을 위해 다른 화면을 여는 횟수
- 오포장·재포장·라벨 재출력 건수
- 신규 작업자 독립작업까지 걸리는 시간
- 상품데이터 오류 때문에 중단된 시간
- 포장 PC별 처리량과 오류율

### 23.5 상업화 가능성 — 외부 판매 관점

이 프로그램을 향후 다른 셀러, 3PL 또는 물류창고에 판매하려면 사내 전용 로직과 판매 가능한 제품 기능을 분리해야 한다.

#### 차별화 포인트

1. Amazon FNSKU 중심의 매우 빠른 상품 조회
2. FNSKU에서 품목코드·Seller SKU·판매국가까지 동시에 확인
3. 다품종 소량포장과 한 박스 다중 FNSKU 합포 특화
4. SharePoint 기반 상품정보 관리와 승인
5. 네트워크가 불안정해도 작동하는 경량 로컬 캐시
6. 기존 USB 스캐너·Windows PC·라벨 프린터 활용
7. WMS 전체를 도입하기 어려운 소형·중형 현장의 낮은 진입장벽

#### 상업화 필수요건

| 요구사항 ID | 구분 | 기준 |
|---|---|---|
| `COM-001` | 고객 분리 | 고객사별 SharePoint·상품DB·설정 완전 분리 |
| `COM-002` | 설정화 | 국가·필드명·라벨·단위·프린터를 코드수정 없이 설정 |
| `COM-003` | 배포 | 서명된 설치파일, 자동 업데이트, 이전 버전 롤백 |
| `COM-004` | 라이선스 | 작업대·사업장·활성사용자 중 과금기준 선택 가능 |
| `COM-005` | 지원 | 진단파일, 오류코드, 원격지원 절차, 지원 이력 |
| `COM-006` | 보안 | 고객별 최소권한·토큰·인증서 분리와 교체 절차 |
| `COM-007` | 데이터 이동성 | 고객이 상품·포장 실적을 표준 형식으로 내보낼 수 있음 |
| `COM-008` | 현지화 | 한국어·영어, 국가·단위·시간대·날짜 형식 지원 |
| `COM-009` | 하드웨어 | 지원 스캐너·프린터·저울 호환성 목록 공개 |
| `COM-010` | 계약 | SLA, 백업책임, 데이터소유권, 지원범위 명시 |
| `COM-011` | 개인정보 | 최소 수집, 보관기간, 삭제·반출 정책 |
| `COM-012` | 확장 API | SharePoint 외 ERP·WMS 상품소스를 어댑터로 추가 가능 |

#### 권장 상품 구성

| 제품 단계 | 대상 | 제공 기능 |
|---|---|---|
| Starter | 1~3개 포장 작업대 | SharePoint 동기화, 로컬 캐시, FNSKU 조회, 합포, 라벨·Excel |
| Professional | 중소형 창고 | 사용자·권한, 중앙 작업실적, 대시보드, 다중 작업대, 자동 업데이트 |
| Enterprise | 대형 창고·3PL | SSO, 다창고, WMS/ERP API, 중앙 감사, SLA, 고가용성 |

초기에는 외부 판매보다 내부 운영에서 정확도와 ROI를 입증하는 것이 우선이다. 내부 파일럿에서 처리시간·오류율·교육시간 개선이 확인된 뒤 가격과 라이선스 모델을 검증한다.

### 23.6 우선순위별 제품 백로그

| 단계 | 목표 | 핵심 내용 |
|---|---|---|
| MVP/P0 | 현장 오류 없이 빠르게 사용 | 자동동기화, 로컬 캐시, FNSKU 조회, 신규 필드, 합포, 자동저장, 명확한 오류 |
| Pilot/P1 | 반복작업과 장애 대응 개선 | 최근값 제안, 템플릿, 박스복제, 단축키, 진단파일, 롤백, 작업자 교육 |
| Product/P2 | 판매 가능한 제품화 | 서명 설치, 자동업데이트, 고객분리, 라이선스, 다국어, 지원체계, 하드웨어 인증 |
| Scale/P3 | 중대형 창고 확장 | 중앙 실적, 다중 작업대, WMS API, 역할·감사, 다창고, SLA |

### 23.7 추가 인수 기준

아래를 만족하지 못하면 “기능 구현 완료”로 처리하지 않는다.

1. 작업자는 정상 포장 20건을 마우스 없이 연속 완료할 수 있다.
2. 신규 작업자는 30분 교육 후 단품과 합포 작업을 독립적으로 수행한다.
3. 잘못된 FNSKU·수량·치수·무게는 저장 전에 발견된다.
4. 프로그램 강제 종료 후 마지막 미완료 작업을 복구한다.
5. 네트워크 단절 중에도 캐시된 상품 포장을 계속할 수 있다.
6. 프린터 오류가 발생해도 포장내용을 다시 입력하지 않고 재출력한다.
7. 작업자가 보는 오류 메시지에 해결방법이 포함된다.
8. 관리자는 진단파일만으로 앱·상품DB·동기화·프린터 버전을 확인한다.
9. 설치·업데이트·롤백 과정에서 사용자 데이터가 손실되지 않는다.
10. 파일럿 전후 작업시간·오류율·교육시간 개선을 수치로 보고한다.

---

## 24. 참고자료

- [Microsoft Graph SharePoint List 항목](https://learn.microsoft.com/en-us/graph/api/resources/listitem?view=graph-rest-1.0)
- [Microsoft Graph List 항목 조회](https://learn.microsoft.com/en-us/graph/api/listitem-list?view=graph-rest-1.0)
- [Microsoft 데스크톱 앱 인증 구성](https://learn.microsoft.com/en-us/entra/identity-platform/scenario-desktop-app-configuration)
- [SharePoint App-Only 인증](https://learn.microsoft.com/en-us/sharepoint/dev/solution-guidance/security-apponly)
- [SharePoint Online 제한·재시도 지침](https://learn.microsoft.com/en-us/sharepoint/dev/general-development/how-to-avoid-getting-throttled-or-blocked-in-sharepoint-online)
- [SharePoint List View Threshold](https://learn.microsoft.com/en-us/troubleshoot/sharepoint/lists-and-libraries/fails-filtering-sharepoint-column)
- [Microsoft 창고 모바일 앱 단계별 제목·지시](https://learn.microsoft.com/en-us/dynamics365/supply-chain/warehousing/mobile-app-titles-instructions)
- [Microsoft 창고 모바일 앱 예외 작업과 복귀](https://learn.microsoft.com/en-us/dynamics365/supply-chain/warehousing/warehouse-app-detours)
- [Odoo Barcode 명령 바코드](https://www.odoo.com/documentation/19.0/applications/inventory_and_mrp/barcode/setup/operation_types.html)
- [Windows 앱 코드서명 선택](https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/code-signing-options)
- [Windows 앱 배포 경로와 자동 업데이트](https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/choose-distribution-path)
- `RESEARCH.MD` — 현행 시스템 분석, 글로벌 벤치마크, 중장기 WMS 확장전략
